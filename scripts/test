#!/bin/bash

npx jest --coverage && npx codecov || exit 1
rm -rf coverage

echo WARNING disabled e2e tests as currently they are getting stuck, probably because of open connection to mongo
# npx jest --config ./jest-e2e.json --coverage && npx codecov || exit 1
# rm -rf coverage

