#!/bin/sh

# update image
kubectl set image deployment/msw-server-deployment msw-server-deployment=myspecialway/msw-server:$(./scripts/echo-version package.json)
# kubectl rolling-update msw-server --image=myspecialway/msw-sever:latest