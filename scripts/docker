#!/bin/bash

docker pull node:carbon

if [$BRANCH = "master"]
then
    docker build . -t myspecialway/msw-server:$(./scripts/echo-version package.json) -t myspecialway/msw-server:latest
else
    docker build . -t myspecialway/msw-server:$BRANCH
fi